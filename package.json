{
  "scripts": {
    "start": "json-server --watch ./database/db.json",
    "dev": "concurrently \"tailwindcss -i ./src/input.css -o ./src/output.css --watch\"",
    "build": "tailwindcss -i ./src/input.css -o ./src/output.css --minify"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.4",
    "json-server": "^1.0.0-beta.3",
    "lucide": "^0.503.0",
    "tailwindcss": "^4.1.4"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
